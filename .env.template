# Docker 环境配置模板
# 复制此文件为 .env 并填入实际值

# 阿里云 DashScope API Key（必须）
DASHSCOPE_API_KEY=your_dashscope_api_key_here

# PostgreSQL 配置（可选，使用默认值）
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123
POSTGRES_DB=novel_agent

# JVM 配置（可选）
JAVA_OPTS=-Xms512m -Xmx1024m